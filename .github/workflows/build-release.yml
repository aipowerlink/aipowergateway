name: build-release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  # 从 tag 提取版本（无 tag 时用 dev）
  APP_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
permissions:
  contents: write   # release job 生成 Release Notes / 创建 Release 需要
  actions: read


jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: aipowergateway-windows-x64
            bin: aipowergateway.exe
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: aipowergateway-linux-x64
            bin: aipowergateway
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: aipowergateway-macos-x64
            bin: aipowergateway
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Rust (MSVC toolchain for Windows)
        if: matrix.os == 'windows-latest'
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-pc-windows-msvc

      - name: Install Rust (Linux/macOS)
        if: matrix.os != 'windows-latest'
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Install Linux system dependencies (tray-icon needs GTK)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libglib2.0-dev libxdo-dev libayatana-appindicator3-dev

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run tests
        run: cargo test --workspace

      - name: Build web console
        working-directory: web
        run: |
          npm ci
          npm run build

      - name: Build release binary
        run: cargo build --release -p aipg-cli

      - name: Package artifact (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $dist = "dist/${{ matrix.artifact }}"
          New-Item -ItemType Directory -Force -Path $dist | Out-Null
          Copy-Item target/release/${{ matrix.bin }} $dist/
          Copy-Item -Recurse web/dist $dist/web
          Copy-Item README.md $dist/
          if (Test-Path LICENSE) { Copy-Item LICENSE $dist/ }
          # PowerShell Compress-Archive 生成真正的 zip
          Compress-Archive -Path $dist -DestinationPath "dist/${{ matrix.artifact }}-${{ env.APP_VERSION }}.zip"

      - name: Package artifact (Linux/macOS)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          mkdir -p dist/${{ matrix.artifact }}
          cp target/release/${{ matrix.bin }} dist/${{ matrix.artifact }}/
          cp -r web/dist dist/${{ matrix.artifact }}/web
          cp README.md dist/${{ matrix.artifact }}/
          cp LICENSE dist/${{ matrix.artifact }}/ 2>/dev/null || true
          cd dist && zip -r ${{ matrix.artifact }}-${{ env.APP_VERSION }}.zip ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: dist/*.zip
          if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v6
        with:
          path: artifacts
          merge-multiple: true
      - name: List downloaded files
        run: find artifacts -type f | sort
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip
          generate_release_notes: true